spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://database-1.cj002sycood8.ap-southeast-2.rds.amazonaws.com:3306/team_project?serverTimezone=Asia/Seoul
    username: dongju
    password: dong1234!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  application:
    name: backend

  web:
    resources:
      add-mappings: false


  autoconfigure:

server:
  port: 8080
  address: 0.0.0.0

musinsa:
  image-base-url: ${MUSINSA_IMAGE_BASE_URL}


